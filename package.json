{
  "name": "growly",
  "private": true,
  "scripts": {
    "dev": "concurrently -n ai-service,backend,frontend -c yellow,cyan,magenta \"npm run dev --prefix ai-service\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:ai-service": "npm run dev --prefix ai-service",
    "dev:ai-studio": "npm run studio --prefix ai-service",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
